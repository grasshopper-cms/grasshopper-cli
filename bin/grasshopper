#!/usr/bin/env node
'use strict';

var program = require('commander'),
    output = require('../lib/output'),
    commands = require('../lib/commands');

program.version('0.0.1')
       .option('setup', 'Setup');

program.on('setup', commands.setup);

output.welcome();
program.parse(process.argv);